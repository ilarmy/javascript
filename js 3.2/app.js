let year_month = prompt('Enter the month number');

if (year_month == 1) {
  alert( 'Bu ay yanvar ayidir' );
} else if (year_month ==12) {
  alert( 'Bu ay dekabr ayidir' );
} else {
  alert( 'Ilin diger ayidir' );
}